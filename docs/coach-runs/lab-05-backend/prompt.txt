Implement SQLite persistence in src/lib/db.ts for this Astro site.

Replace the stub implementations of insertContact, listGuestbook, and insertGuestbook so they use getDb() and better-sqlite3.

Rules:
- insertContact({name,email,message}): validate non-empty strings and basic email containing @; insert into contact_messages; return the row with id and created_at
- insertGuestbook({name,message}): validate non-empty; insert into guestbook; return row
- listGuestbook(): return all guestbook rows ordered by id desc
- Keep getDb() as is
- Do not change API route files unless required
- Ensure npm run test:labs will pass

After edits, summarize what you changed.
