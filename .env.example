# === Lab harness env (optional local ports for Command Center) ===
# Secrets ของสินค้าอยู่ที่ repo ที่สร้างจาก course-personal-site ไม่ใช่ที่นี่

# Claude Agent Teams (Windows = in-process, ห้ามบังคับ tmux)
CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1

# Optional Command Center watch UI
COMMAND_CENTER_PORT=4181

# ไม่ใช้ Trade Desk / Actual Budget ใน V4 แล้ว
