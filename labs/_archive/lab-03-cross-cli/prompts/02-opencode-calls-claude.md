# Lab 03 prompt — OpenCode calls Claude (ทางเลือก)

1. โหลด skill `dispatch-claude` และ `log-dispatch`
2. จาก OpenCode เรียก:

```bash
claude -p --agent frontend "ปรับปุ่ม Place paper order ให้แสดง loading ขณะรอ response"
```

3. บันทึก dispatch (from=opencode, to=claude)
4. ผู้เรียนรีเฟรช Command Center
5. หยุดแล้วรอผู้เรียนสั่งต่อเอง

---

นี่คือ**โครงเริ่ม** ไม่ใช่คำตอบสำเร็จรูป  
ผู้เรียนสั่งต่อได้ — ลูกศรต้องมาจากเครื่องเขา
