อ่าน COURSE.md และ issue Lab 01

1) เข้า Plan mode — โฟกัสไฟล์ packages/desktop-client/src/components/accounts/AccountEmptyMessage.tsx
2) ใช้ Explore ยืนยันว่า empty state ถูกเรนเดอร์จากคอมโพเนนต์นี้
3) ใส่ข้อความไทยใน empty state: ยังไม่มีบัญชี — เพิ่มบัญชีตัวอย่างเพื่อเริ่มงบประมาณกระดาษ (คงปุ่ม Add account ไว้)
4) เปิด PR เข้า default branch ของ repo นี้เท่านั้น ผ่าน GitHub MCP
5) ใน PR body เขียน: แผนสั้น + เปรียบเทียบ GitHub MCP กับคำสั่ง gh ที่เทียบเท่า (เมื่อไหร่ควรใช้ MCP)
6) ถ้า localhost:3001 ขึ้น BackendInitFailure บน Windows ให้บันทึกใน PR แล้วยัง ship diff ได้
7) ห้าม bank sync จริง ห้าม commit ความลับ
