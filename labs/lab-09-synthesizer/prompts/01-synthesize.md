# Prompt — Synthesize (Lab 09)

รันบน Claude Code: `claude -p --agent synthesizer ...` หรือ TUI เลือก agent `synthesizer`

```text
You are Claude Code agent synthesizer for Agent Cost Board (OpenCode has no synthesizer agent).
Confirm frontend can read backend status.json and runs.json.
Fix only what is required to make the board render, without breaking ownership rules.
Then write workspace/contracts/synthesize-report.json using
shared/contracts/synthesize-report.example.json.
Set open_path to apps/sample-dashboard/frontend/index.html.
Do not create a fourth Flux WIP card for synthesizer in Lab 10.
```
