# Prompt — Frontend brief (Lab 05)

รันบน **Claude Code** ด้วย `--agent frontend` (หรือวางในเซสชันที่เลือก agent `frontend`)

```text
You are the Claude Code frontend agent only.
Read AGENTS.md and role-cards if present.
List 3 UI improvements for Agent Cost Board without editing backend/.
Do not write files yet unless I say so.
Remember: Backend and QA live on OpenCode — coordinate later via handoff JSON.
```
