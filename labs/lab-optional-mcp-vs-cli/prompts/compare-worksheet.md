# Worksheet: MCP / API สำเร็จรูป vs CLI ของคอร์ส

คัดลอกบล็อกด้านล่างไป `workspace/learning-log.md` แล้วเติมคำตอบ

```md
### Lab เสริม — MCP vs CLI (optional)

- วันที่:
- เครื่องมือที่ใช้ (Claude Code / OpenCode):
- Track ที่ทำ: A (MCP) / B (เอกสารอย่างเดียว)

#### Baseline CLI
- คำสั่งที่รัน:
- ได้ `cache_ref` หรือไม่:
- โดน rate limit / 429 หรือไม่:

#### ถ้าทำ Track A (MCP)
- ชื่อ MCP / provider:
- ชื่อ tool ที่เรียก:
- ต้องใช้ API key หรือไม่:
- ผลที่ได้สรุปสั้น ๆ (ไม่ต้องวาง JSON ยาว):

#### เปรียบเทียบ
| มุม | CLI + cache ของคอร์ส | MCP / API สำเร็จรูป |
|---|---|---|
| ความง่ายตอนเรียก | | |
| ความเสี่ยง rate limit ทั้งห้อง | | |
| ต้องสมัคร key / billing | | |
| ทำซ้ำได้เมื่อเน็ตพัง | | |
| เข้ากับ JSON contract | | |

#### คำถามบังคับ (ตอบสั้น ๆ)
1. ทำไมคอร์สนี้ใช้ `shared/scripts/tools/*` + `external-cache` เป็นค่าเริ่มต้น?
   - คำตอบต้องพูดถึง **rate limit** อย่างน้อยหนึ่งประโยค
2. ถ้ามี MCP สวยกว่า ทำไมยังไม่ย้ายเกณฑ์ผ่าน Lab 2 ทั้งห้องไปพึ่ง MCP อย่างเดียว?
3. ไม่ว่าจะเรียกจาก CLI หรือ MCP ผลต้องไปอยู่ที่ไหนก่อนส่งต่อ Agent ถัดไป?
```
