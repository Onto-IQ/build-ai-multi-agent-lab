# Prompt — แก้แล้วตรวจใหม่ (Lab 08)

```text
Fix apps/sample-dashboard/backend/status.json so gate-quality can pass.
Keep this within refinement round 2 maximum.
Then run: node shared/scripts/gate-quality.mjs
Update workspace/contracts/audit-result.json to PASS if the gate passes.
If you would need a 3rd refinement round, stop and ask the human instead.
```
