# Prompt — Kanban snapshot (Lab 10)

```text
Create workspace/contracts/kanban-snapshot.json for the Agent Cost Board Flux board.
Use shared/contracts/kanban-snapshot.example.json as the schema.
board_tool should be Flux (or the tool you actually used).
Include at least 3 cards with assignee_role and column.
columns must be Interview, Plan, Build, Test, Ship.
Then validate the file with node shared/scripts/validate-json.mjs.
```
