# Prompt — บันทึก learning-log (Lab 10)

วางใน TUI (Claude) หลัง snapshot จากบอร์ดสด — หรือใช้กับ CLI:

```text
Append "## Lab 10" to workspace/learning-log.md.
Record: go-live loop done; exactly 3 active cards (FE/BE/QA);
named agents used (frontend on Claude, backend+qa on OpenCode);
paste the live FLUX_WORKSPACE_URL.
Note that Kanban assigns work but JSON contracts remain source of truth.
Short Thai. Only edit workspace/learning-log.md.
```

> [`05-kanban-snapshot.md`](05-kanban-snapshot.md) ก็สั่งอัป learning-log แล้ว — ใช้ไฟล์นี้ถ้าต้องการรอบแยกเฉพาะ log / แปะ URL
