# Prompt — Claude รับการ์ด Frontend (Lab 10, go-live)

รันด้วย Claude `--agent frontend`

```text
Go-live Lab 10. You are Claude Code agent "frontend" on Agent Cost Board.
1) Flux MCP: find the single Frontend card; confirm Assignee=Frontend, Tool=Claude Code, Agent=frontend.
2) Read role-cards.json and handoff-fe.json. Write only frontend/ (+ contracts you own).
3) Do REAL work: small UI clarity improvement OR verify board UI is ready with evidence.
4) Update handoff-fe.json to match reality.
5) ONLY THEN move_card (target_column_id) to match status (e.g. Build→Test).
   Never move without work/verify evidence.
6) Do not create extra cards. Do not impersonate backend/qa (those are OpenCode-only).

Reply Thai: evidence (files) + old→new column.
```
